//
// Created by davidrocha on 22/05/20.
//

#include <iostream>
#include <algorithm>
#include "yen.h"
#include "dijkstra.h"

vector<vector<int>> yen(Graph g, int src, int dest, int k){
    /*vector<vector<int>> A, B;
    A.push_back(dijkstraShortestPath(g, src, dest));

    for (size_t x = 1; x <= k; x++){
        for (size_t y = 0; y < A.at(k-1).size() - 2; y++){
            Vertex* spurNode = g.findVertex(A.at(k-1).at(y));
            vector<int> rootPath;
            for (int z = 0; z < y; z++){
                rootPath.push_back(A.at(k-1).at(z));
            }

            for (auto path: A){
                if (path == rootPath){

                }
            }
        }
    }

    return A;*/
}